# Preview Branch

This branch is a preview branch. You can preview the front-end mechanics and visual design without the backend apis set up.

## Running

You have already checked out the preview branch with:

```bash
git checkout preview
```

You then decide to copy over the `.env.preview`:

```bash
cp .env.preview .env.local
```

